# 🚀 Деплой на Vercel + Railway БД

## Комбинация:
- ✅ **Vercel** - для Next.js приложения (бесплатно)
- ✅ **Railway** - для PostgreSQL БД (уже создана)

---

## Шаг 1: Получить строку подключения из Railway

1. Открой [railway.app](https://railway.app)
2. Открой свой проект с PostgreSQL
3. Нажми на карточку **Postgres**
4. Перейди на вкладку **Variables**
5. Найди `DATABASE_URL`
6. **Скопируй** эту строку (нажми на иконку Copy)

Строка выглядит примерно так:
```
postgresql://postgres:пароль@containers-us-west-123.railway.app:5432/railway
```

**Сохрани её** - понадобится дважды!

---

## Шаг 2: Применить миграции к Railway БД

Откой терминал в папке проекта:

```bash
cd /Users/nikolaybazhan/Desktop/KuznecChess
```

### 2.1. Создай файл `.env`

```bash
nano .env
```

Вставь DATABASE_URL из Railway:
```env
DATABASE_URL="postgresql://postgres:твой_пароль@containers-us-west-123.railway.app:5432/railway"
```

Сохрани: **Ctrl+O**, **Enter**, **Ctrl+X**

### 2.2. Примени миграции

```bash
# Установи зависимости
npm install

# Примени миграции к БД
npx prisma migrate deploy

# Сгенерируй Prisma Client
npx prisma generate

# Заполни тестовыми данными
npm run db:seed
```

✅ **БД настроена и заполнена!**

Проверь (опционально):
```bash
npx prisma studio
```
Откроется браузер с данными.

---

## Шаг 3: Запушить код на GitHub

### 3.1. Инициализируй Git

```bash
# Инициализировать (если еще не сделано)
git init

# Добавить все файлы
git add .

# Первый коммит
git commit -m "Initial commit: Chess tournament app"
```

### 3.2. Создай репозиторий на GitHub

1. Зайди на [github.com](https://github.com)
2. Нажми **New repository** (зеленая кнопка)
3. Название: `kuznec-chess` (или любое)
4. Visibility: **Public** или **Private**
5. **НЕ** добавляй README, .gitignore (уже есть)
6. Нажми **Create repository**

### 3.3. Запушь код

GitHub покажет команды, скопируй и выполни:

```bash
git remote add origin https://github.com/твой-username/kuznec-chess.git
git branch -M main
git push -u origin main
```

✅ **Код на GitHub!**

---

## Шаг 4: Деплой на Vercel

### 4.1. Подключить GitHub к Vercel

1. Открой [vercel.com](https://vercel.com)
2. Нажми **Add New** → **Project**
3. **Import Git Repository** → выбери `kuznec-chess`
4. Нажми **Import**

### 4.2. Настроить Environment Variables

**ВАЖНО!** Нужно добавить `DATABASE_URL` из Railway:

На странице настройки проекта:

1. Разверни секцию **Environment Variables**
2. Добавь переменную:
   - **Key**: `DATABASE_URL`
   - **Value**: `postgresql://postgres:твой_пароль@containers-us-west-123.railway.app:5432/railway` (из шага 1)
   - **Environments**: отметь **Production**, **Preview**, **Development**
3. Нажми **Add**

### 4.3. Deploy

Нажми **Deploy** внизу страницы.

Vercel автоматически:
- ✅ Скачает код из GitHub
- ✅ Установит зависимости
- ✅ Соберет приложение
- ✅ Применит миграции (благодаря `postinstall: "prisma generate"`)
- ✅ Задеплоит

Подожди 1-2 минуты...

✅ **Деплой завершен!**

---

## Шаг 5: Получить URL и проверить

После деплоя:

1. Vercel покажет **Congratulations** 🎉
2. Нажми **Visit** или скопируй URL
3. Откроется твой сайт, например:
   ```
   https://kuznec-chess.vercel.app
   ```

Проверь:
- Войди как `nickolay@chess.com`
- Попробуй записать результат партии
- Перезагрузи страницу - данные должны сохраниться

✅ **Всё работает!**

---

## 📝 Обновление приложения

После любых изменений в коде:

```bash
# 1. Закоммить
git add .
git commit -m "Описание изменений"

# 2. Запушить на GitHub
git push

# Vercel автоматически задеплоит новую версию!
```

---

## 🔧 Если что-то не работает

### Проблема: "Can't connect to database"

**Решение:**
1. Проверь, что `DATABASE_URL` добавлен в Vercel Environment Variables
2. В Vercel Dashboard → твой проект → **Settings** → **Environment Variables**
3. Убедись, что значение правильное (скопируй из Railway заново)
4. **Redeploy**: Deployments → последний деплой → три точки → **Redeploy**

### Проблема: "Prisma schema not found"

**Решение:**
Уже должно работать, но если нет:
- Vercel Dashboard → Settings → **Environment Variables**
- Добавь `PRISMA_GENERATE_SKIP_AUTOINSTALL=false`
- Redeploy

### Проблема: "Empty tables in database"

**Решение:**
Запусти seed заново (локально):
```bash
npm run db:seed
```

---

## 💰 Стоимость

- **Vercel**: Полностью **бесплатно** для твоего проекта
- **Railway PostgreSQL**: ~$2-3/месяц (покрывается бесплатными $5 кредитами)

**Итого: БЕСПЛАТНО!** 🎉

---

## 🎯 Итоговый чеклист

- [ ] Скопировать `DATABASE_URL` из Railway
- [ ] Создать `.env` локально
- [ ] Применить миграции: `npx prisma migrate deploy`
- [ ] Заполнить данными: `npm run db:seed`
- [ ] Запушить на GitHub
- [ ] Подключить GitHub к Vercel
- [ ] Добавить `DATABASE_URL` в Vercel Environment Variables
- [ ] Deploy
- [ ] Проверить работу

---

## 📞 Полезные ссылки

- Railway Dashboard: https://railway.app/dashboard
- Vercel Dashboard: https://vercel.com/dashboard
- GitHub: https://github.com/твой-username/kuznec-chess

---

**Powered by Kuznec production** 🦗

