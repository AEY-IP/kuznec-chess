# 🚂 Деплой на Railway - Пошаговая инструкция

## Что нужно:
- ✅ Аккаунт на [railway.app](https://railway.app) (бесплатно)
- ✅ Git установлен
- ✅ Проект готов к деплою

---

## Шаг 1: Создать PostgreSQL БД в Railway

1. Открой [railway.app](https://railway.app)
2. Нажми **New Project**
3. Выбери **Deploy PostgreSQL**
4. Подожди 1-2 минуты, пока БД создастся

✅ **БД создана!**

---

## Шаг 2: Получить строку подключения

1. В Railway Dashboard нажми на **Postgres** (карточка БД)
2. Перейди на вкладку **Variables**
3. Найди переменную `DATABASE_URL`
4. Нажми на иконку **Copy** справа
5. **Сохрани** эту строку, она понадобится!

Строка выглядит так:
```
postgresql://postgres:пароль@хост:порт/railway
```

---

## Шаг 3: Применить миграции к БД

### 3.1. Создай файл `.env` локально

```bash
cd /Users/nikolaybazhan/Desktop/KuznecChess

# Создай .env файл
nano .env
```

Вставь в него:
```env
DATABASE_URL="твоя_строка_из_Railway"
```

Сохрани (Ctrl+O, Enter, Ctrl+X)

### 3.2. Примени миграции

```bash
# Установи зависимости (если еще не установлены)
npm install

# Примени миграции
npx prisma migrate deploy

# Сгенерируй Prisma Client
npx prisma generate

# Заполни БД тестовыми данными
npm run db:seed
```

✅ **БД настроена!**

Проверь, что данные появились:
```bash
npx prisma studio
```

Откроется браузер с визуальным редактором БД.

---

## Шаг 4: Подключить GitHub репозиторий

### 4.1. Инициализируй Git (если еще не сделано)

```bash
cd /Users/nikolaybazhan/Desktop/KuznecChess

# Инициализировать Git
git init

# Добавить все файлы (кроме .env - он в .gitignore)
git add .

# Сделать первый коммит
git commit -m "Initial commit: Chess tournament app"
```

### 4.2. Создай репозиторий на GitHub

1. Зайди на [github.com](https://github.com)
2. Нажми **New repository**
3. Название: `kuznec-chess` (или любое другое)
4. Visibility: **Public** или **Private**
5. **НЕ** добавляй README, .gitignore (они уже есть)
6. Нажми **Create repository**

### 4.3. Запуш код на GitHub

Скопируй команды из GitHub (раздел "...or push an existing repository from the command line"):

```bash
git remote add origin https://github.com/твой-username/kuznec-chess.git
git branch -M main
git push -u origin main
```

✅ **Код на GitHub!**

---

## Шаг 5: Задеплоить на Railway

### 5.1. Подключить GitHub к Railway

1. Вернись в Railway Dashboard
2. Нажми **New** → **Deploy from GitHub repo**
3. Если первый раз, разреши доступ к GitHub
4. Выбери репозиторий `kuznec-chess`
5. Нажми **Deploy**

Railway автоматически:
- ✅ Скачает код
- ✅ Установит зависимости (`npm install`)
- ✅ Соберет приложение (`npm run build`)
- ✅ Запустит сервер

### 5.2. Добавить переменные окружения

**ВАЖНО!** Нужно добавить `DATABASE_URL` в Railway:

1. В Railway Dashboard нажми на карточку твоего **App** (не Postgres!)
2. Перейди на вкладку **Variables**
3. Нажми **+ New Variable**
4. **Variable Reference** → выбери `DATABASE_URL` из **Postgres**
5. Сохрани

Альтернативно (вручную):
- Имя: `DATABASE_URL`
- Значение: твоя строка подключения из шага 2

### 5.3. Применить миграции на сервере

```bash
# В Railway Dashboard → твой App → Settings → Deploy Trigger
# Или просто сделай новый коммит:

git commit --allow-empty -m "Trigger redeploy"
git push
```

Railway автоматически применит миграции благодаря `postinstall` скрипту в `package.json`.

---

## Шаг 6: Получить URL приложения

1. В Railway Dashboard нажми на карточку **App**
2. Перейди на вкладку **Settings**
3. Прокрути до **Domains**
4. Нажми **Generate Domain**

Railway создаст домен вида:
```
https://kuznec-chess-production.up.railway.app
```

✅ **Приложение задеплоено!**

---

## Шаг 7: Проверить работу

1. Открой сгенерированный URL
2. Попробуй войти (например, `nickolay@chess.com`)
3. Проверь, что данные сохраняются

Если что-то не работает, проверь логи:
- Railway Dashboard → App → **Deployments** → последний деплой → **View Logs**

---

## 📝 Команды для обновления

После любых изменений в коде:

```bash
# 1. Закоммить изменения
git add .
git commit -m "Описание изменений"

# 2. Запушить
git push

# Railway автоматически задеплоит новую версию!
```

---

## 🔧 Частые проблемы

### Ошибка: "Can't reach database"

**Решение:**
1. Проверь, что `DATABASE_URL` добавлен в Variables (шаг 5.2)
2. Убедись, что БД и App в одном проекте Railway
3. Перезапусти деплой

### Ошибка: "Prisma schema not found"

**Решение:**
```bash
# Добавь в package.json, если еще нет:
"postinstall": "prisma generate"
```

Уже должно быть! Просто сделай редеплой:
```bash
git commit --allow-empty -m "Redeploy"
git push
```

### Данные не сохраняются

**Решение:**
Примени миграции заново:
```bash
# Локально с твоим DATABASE_URL из Railway
npx prisma migrate deploy
npm run db:seed
```

---

## 💰 Стоимость

Railway дает **$5 кредитов в месяц** бесплатно.

Для твоего проекта:
- PostgreSQL: ~$2-3/месяц
- App (Next.js): ~$1-2/месяц

**Итого:** ~$3-5/месяц = **покрывается бесплатными кредитами!** 🎉

Если кредиты закончатся, можно:
- Добавить карту (списания не будет, пока не превысишь лимит)
- Перейти на Vercel (бесплатный Next.js) + Supabase (бесплатный Postgres)

---

## 🎯 Итоговый чеклист

- [ ] Создать PostgreSQL в Railway
- [ ] Скопировать DATABASE_URL
- [ ] Применить миграции локально
- [ ] Запушить код на GitHub
- [ ] Подключить GitHub к Railway
- [ ] Добавить DATABASE_URL в Variables
- [ ] Сгенерировать домен
- [ ] Проверить работу

---

## 📞 Нужна помощь?

- Railway Docs: https://docs.railway.app
- Discord Railway: https://discord.gg/railway
- Или спроси меня! 😊

**Powered by Kuznec production** 🦗

