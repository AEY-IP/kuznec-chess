# 🗄️ Настройка базы данных

## Текущее состояние

✅ Prisma ORM установлен
✅ Схема БД создана (`prisma/schema.prisma`)
✅ Адаптер готов (`lib/db-adapter.ts`)
✅ Гибридный режим: работает с БД или без неё

## Как это работает?

### Без базы данных (по умолчанию)
```bash
npm run dev
```

- Данные хранятся в памяти
- Работает сразу "из коробки"
- Результаты исчезают при перезапуске
- **Подходит для**: локальной разработки, тестирования

### С базой данных
```bash
# 1. Запустить PostgreSQL
docker run --name postgres-chess \
  -e POSTGRES_PASSWORD=chess123 \
  -e POSTGRES_DB=chess_tournament \
  -p 5432:5432 \
  -d postgres:16

# 2. Настроить .env
echo 'DATABASE_URL="postgresql://postgres:chess123@localhost:5432/chess_tournament"' > .env

# 3. Применить миграции
npx prisma migrate dev --name init
npx prisma generate
npm run db:seed

# 4. Запустить
npm run dev
```

- Данные хранятся в PostgreSQL
- Сохраняются при перезапуске
- **Подходит для**: продакшен, деплоя, совместной работы

## Структура БД

```
┌─────────────┐
│    User     │
├─────────────┤
│ id          │──┐
│ email       │  │
│ username    │  │
│ nickname    │  │
│ role        │  │
└─────────────┘  │
                 │
┌─────────────┐  │
│ Tournament  │  │
├─────────────┤  │
│ id          │──┤
│ name        │  │
│ stage       │  │
│ participants│  │
└─────────────┘  │
       │         │
       │         │
┌─────────────┐  │
│   Match     │  │
├─────────────┤  │
│ id          │  │
│ tournamentId│──┘
│ player1Id   │──┐
│ player2Id   │──┤
│ stage       │  │
│ round       │  │
│ status      │  │
│ result (JSON)│ │
└─────────────┘  │
                 │
                 └──> связь Many-to-One
```

## Важные файлы

| Файл | Описание |
|------|----------|
| `prisma/schema.prisma` | Схема БД (таблицы, поля, связи) |
| `lib/prisma.ts` | Prisma Client (singleton) |
| `lib/db-adapter.ts` | Адаптер (автоматически выбирает БД или память) |
| `lib/storage.ts` | In-memory хранилище (fallback) |
| `prisma/seed.ts` | Скрипт для заполнения БД |
| `.env` | Переменные окружения (DATABASE_URL) |

## Частые вопросы

### Q: Как переключиться с памяти на БД?
A: Просто добавь `DATABASE_URL` в `.env` и перезапусти сервер.

### Q: Можно ли использовать другую БД (MySQL, SQLite)?
A: Да, измени `provider` в `prisma/schema.prisma`:
```prisma
datasource db {
  provider = "sqlite"  // или "mysql"
  url      = env("DATABASE_URL")
}
```

### Q: Как посмотреть данные в БД?
A: Запусти `npm run db:studio` - откроется визуальный редактор.

### Q: Как сбросить БД?
A: `npx prisma migrate reset` (ОСТОРОЖНО! Удалит все данные)

### Q: Нужна ли БД для деплоя?
A: Да, для продакшена обязательно используй БД. Рекомендуем:
- **Vercel Postgres** (интеграция с Vercel)
- **Supabase** (бесплатный PostgreSQL)
- **Railway** (простой деплой)

## Деплой с БД

См. подробную инструкцию в:
- 📖 [QUICK_START.md](./QUICK_START.md) - быстрый старт
- 📖 [DEPLOY.md](./DEPLOY.md) - полная инструкция по деплою

## Поддержка

Если возникли проблемы:
1. Проверь, что Docker контейнер запущен: `docker ps`
2. Проверь строку подключения в `.env`
3. Примени миграции: `npx prisma migrate dev`
4. Сгенерируй клиент: `npx prisma generate`

---

**Powered by Kuznec production** 🦗

