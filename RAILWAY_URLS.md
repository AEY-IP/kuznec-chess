# 🔗 Railway: Внутренний vs Публичный URL

## Проблема

У Railway есть **2 типа** URL для БД:

### 1. Внутренний (Internal) ❌
```
postgresql://postgres:пароль@postgres.railway.internal:5432/railway
```
- Работает **ТОЛЬКО** внутри Railway сети
- Используется если у тебя **несколько сервисов в Railway** (например, App + БД)
- **НЕ работает** из Vercel или локально

### 2. Публичный (Public/External) ✅
```
postgresql://postgres:пароль@containers-us-west-123.railway.app:6543/railway
```
- Работает **откуда угодно**
- Используется для **внешнего доступа** (Vercel, локальная машина)
- Это то, что нам нужно!

---

## 🎯 Как получить публичный URL

### Вариант 1: Через вкладку "Connect"

1. Открой [railway.app/dashboard](https://railway.app/dashboard)
2. Открой свой проект
3. Нажми на карточку **Postgres**
4. Перейди на вкладку **Connect**
5. Найди секцию **Public Networking** или просто строку подключения
6. Скопируй **Postgres Connection URL**

### Вариант 2: Через Variables

1. Открой **Postgres** → вкладка **Variables**
2. Найди переменные:
   - `PGHOST` - там будет `containers-us-west-XXX.railway.app`
   - `PGPORT` - порт (например, `6543`)
   - `PGPASSWORD` - пароль
   - `PGDATABASE` - `railway`
   - `PGUSER` - `postgres`

3. Собери строку вручную:
```
postgresql://postgres:PGPASSWORD@PGHOST:PGPORT/railway
```

Например:
```
postgresql://postgres:wjytvFZrOuyaxppopKZMKOojZzrKTKxr@containers-us-west-123.railway.app:6543/railway
```

---

## ✅ Проверка правильного URL

Правильный URL должен содержать:
- ✅ `containers-us-west-XXX.railway.app` (или другой регион)
- ✅ Порт (обычно 4-5 цифр, например `6543`)
- ❌ **НЕ** должен содержать `postgres.railway.internal`

---

## 🔧 Что делать дальше

После получения правильного URL:

### 1. Обнови `.env` файл

```bash
cd /Users/nikolaybazhan/Desktop/KuznecChess
nano .env
```

Вставь правильный URL:
```env
DATABASE_URL="postgresql://postgres:wjytvFZrOuyaxppopKZMKOojZzrKTKxr@containers-us-west-123.railway.app:6543/railway"
```

Сохрани: **Ctrl+O**, **Enter**, **Ctrl+X**

### 2. Примени миграции

```bash
npm install
npx prisma migrate deploy
npx prisma generate
npm run db:seed
```

### 3. Проверь подключение

```bash
npx prisma studio
```

Если откроется браузер с данными - **всё правильно!** ✅

Если ошибка подключения - URL неправильный, проверь еще раз.

---

## 🚀 После настройки

Используй **тот же публичный URL** в:
1. ✅ Локальный `.env` файл
2. ✅ Vercel Environment Variables

---

**Нужна помощь?** Покажи скриншот вкладки **Connect** из Railway, помогу найти правильный URL!

