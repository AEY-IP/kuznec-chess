# ♟️ Шахматный турнир - Система учета

Веб-приложение для управления шахматным турниром с групповым этапом и финальной сеткой (double-elimination bracket).

## 🎯 Возможности

- ✅ Регистрация и авторизация участников
- ✅ Групповой тур (каждый с каждым по 2 игры)
- ✅ Финальный тур (верхняя и нижняя сетка + финал)
- ✅ Запись результатов с подтверждением соперником
- ✅ Автоматический подсчет очков и рейтинга
- ✅ Красивый современный UI с анимациями
- ✅ Адаптивный дизайн для всех устройств

## 🚀 Быстрый старт

### Локальная разработка (без БД)

```bash
# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

Приложение запустится на http://localhost:3000

Данные будут храниться в памяти (исчезнут при перезапуске).

### С базой данных (для продакшена)

#### 1. Установить PostgreSQL

**Вариант A: Docker (рекомендуется)**
```bash
docker run --name postgres-chess \
  -e POSTGRES_PASSWORD=chess123 \
  -e POSTGRES_DB=chess_tournament \
  -p 5432:5432 \
  -d postgres:16
```

**Вариант B: Локальная установка**
```bash
# macOS
brew install postgresql@16
brew services start postgresql@16
createdb chess_tournament

# Ubuntu/Debian
sudo apt install postgresql-16
sudo -u postgres createdb chess_tournament
```

#### 2. Настроить переменные окружения

```bash
# Скопировать пример
cp .env.example .env

# Отредактировать .env и добавить DATABASE_URL
DATABASE_URL="postgresql://postgres:chess123@localhost:5432/chess_tournament"
```

#### 3. Применить миграции

```bash
# Создать таблицы в БД
npx prisma migrate dev --name init

# Сгенерировать Prisma Client
npx prisma generate

# (Опционально) Заполнить тестовыми данными
npx prisma db seed
```

#### 4. Запустить

```bash
npm run dev
```

## 📊 Структура турнира

### Групповой тур
- 8 участников
- Каждый играет с каждым по 2 игры (1 за белых, 1 за черных)
- Победа = 1 очко, ничья = 0.5 очка, поражение = 0 очков
- Топ-4 проходят в финальный тур

### Финальный тур
- **Верхняя сетка**: топ-2 из группы
- **Нижняя сетка**: 3-4 места
- **Финал**: победитель верхней сетки vs победитель нижней
- **Матч за 3-е место**

## 🎨 Технологии

- **Frontend**: Next.js 14 (App Router), React, TypeScript
- **Styling**: Tailwind CSS
- **Database**: PostgreSQL + Prisma ORM
- **Deployment**: Vercel
- **Font**: Manrope

## 🗂️ Структура проекта

```
├── app/                    # Next.js app router
│   ├── home/              # Главная страница
│   ├── group-stage/       # Групповой тур
│   ├── final-stage/       # Финальный тур
│   ├── pending-matches/   # Партии к подтверждению
│   └── login/             # Авторизация
├── components/            # React компоненты
├── lib/                   # Бизнес-логика
│   ├── storage.ts        # In-memory storage
│   ├── prisma.ts         # Prisma client
│   ├── db-adapter.ts     # Адаптер БД
│   ├── tournament.ts     # Логика турнира
│   └── auth.ts           # Авторизация
├── prisma/               # Схема БД
│   └── schema.prisma
└── types/                # TypeScript типы
    └── index.ts
```

## 📦 Деплой

Подробная инструкция в [DEPLOY.md](./DEPLOY.md)

### Быстрый деплой на Vercel

```bash
# Установить Vercel CLI
npm i -g vercel

# Залогиниться
vercel login

# Задеплоить
vercel --prod
```

После деплоя:
1. Добавить PostgreSQL БД в Vercel (Storage → Postgres)
2. Применить миграции: `npx prisma migrate deploy`

## 🔐 Тестовые аккаунты

- nickolay@chess.com
- sergey@chess.com
- elizabeth@chess.com
- pavel@chess.com
- roman@chess.com
- polina@chess.com
- alexander@chess.com
- alexey@chess.com

При первом входе можно выбрать никнейм.

## 🛠️ Полезные команды

```bash
# Разработка
npm run dev          # Запуск dev сервера
npm run build        # Сборка для продакшена
npm run start        # Запуск продакшен сборки

# База данных
npx prisma studio    # Визуальный редактор БД
npx prisma migrate dev --name название  # Создать миграцию
npx prisma db push   # Применить схему к БД без миграции
npx prisma db seed   # Заполнить тестовыми данными

# Деплой
vercel --prod        # Задеплоить на Vercel
```

## 📝 Цветовая схема

- **Primary**: `#241BDE` (темно-синий)
- **Mint**: `#5EDBBE` (мятный)
- **Cyan**: `#5ECFDB` (голубой)
- **Light Blue**: `#5FA7DC`, `#3B66DB`

## 🤝 Contributing

1. Fork проекта
2. Создать feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Открыть Pull Request

## 📄 License

MIT

## 👨‍💻 Автор

Powered by **Kuznec production** 🦗

---

Сделано с ♟️ и ❤️
